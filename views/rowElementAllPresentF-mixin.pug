mixin rowElementAllPresentF(reqmems, allPresent, day, index)
	if (allPresent[day][index])
		td.allpresentF
			ul 
				each val in reqmems
					+rowElementDisplay(index, val[day], val.name)
	else 
		td
			ul 
				each val in reqmems
					+rowElementDisplay(index, val[day], val.name)